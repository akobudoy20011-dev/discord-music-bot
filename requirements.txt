discord.py[voice]>=2.4.0
# Use the bot owner's maintained yt-dlp fork. The fork already pins the
# matching yt-dlp-ejs package in its [default] extra and exposes a dedicated
# [deno] extra for the JavaScript runtime used by YouTube EJS challenge solving.
yt-dlp[default,deno] @ git+https://github.com/akobudoy20011-dev/yt-dlp.git@master
aiohttp>=3.9.0
python-dotenv>=1.0.0
aiosqlite>=0.20.0
PyNaCl>=1.5.0
google-genai
httpx>=0.25.0
